BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TMP_Logs] ADD CONSTRAINT [TMP_Logs_createdAt_df] DEFAULT CURRENT_TIMESTAMP FOR [createdAt];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
